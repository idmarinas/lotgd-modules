{% trans_default_domain textDomain %}

{{ 'section.run.current.user'|trans(_context)|colorize }}

<br>

<form action="{{ formSearch|lotgd_url }}" method="post">
    <div class="field">
        <label for="name">{{ 'section.run.form.label.name'|trans|colorize }}</label>
        <div class="ui action input">
            <input type="text" name="name" id="name">
            <button class="ui button" type="submit">{{ 'section.run.form.button.submit'|trans|uncolorize }}</button>
        </div>
    </div>
</form>

{% if isSearch %}
    <table class="ui very compact striped table">
        <thead>
            <tr>
                <th scope="col">{{ 'section.run.table.thead.op'|trans|uncolorize }}</th>
                <th scope="col">{{ 'section.run.table.thead.acctid'|trans|uncolorize }}</th>
                <th scope="col">{{ 'section.run.table.thead.login'|trans|uncolorize }}</th>
                <th scope="col">{{ 'section.run.table.thead.name'|trans|uncolorize }}</th>
                <th scope="col">{{ 'section.run.table.thead.level'|trans|uncolorize }}</th>
            </tr>
        </thead>
        <tbody>
            {% for character in paginator %}
                <tr>
                    <td><a href="{{ ('runmodule.php?module=allprefseditor&userid=' ~ character.acctid)|lotgd_url }}">{{ 'section.run.table.tbody.edit'|trans|colorize }}</a></td>
                    <td>{{ character.acctid|numeral }}</td>
                    <td>{{ character.login }}</td>
                    <td>{{ character.name|colorize }}</td>
                    <td>{{ character.level|numeral }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endif %}
