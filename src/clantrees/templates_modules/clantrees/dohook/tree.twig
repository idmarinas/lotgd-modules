{% trans_default_domain textDomain %}

{% if isBestTree %}
    {{ 'section.hook.tree.best'|trans(_context)|colorize }}
{% endif %}

{% if bonus %}
    {{ 'section.hook.tree.bonus.paragraph'|trans(_context)|colorize }}

    {% if competitive %}
        {% if isBestTree %}
            {{ 'section.hook.tree.bonus.best'|trans(_context)|colorize }}
        {% else %}
            {{ 'section.hook.tree.bonus.nobest'|trans(_context)|colorize }}
        {% endif %}
    {% endif %}
{% endif %}
