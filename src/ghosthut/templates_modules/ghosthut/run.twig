{% trans_default_domain textDomain %}

{{ 'subtitle'|trans(_context)|colorize }}

{{ include('@module/ghosthut/run/' ~ tpl|default('default') ~ '.twig') }}

{% if turn == 1 %}
    {{ 'section.turn'|trans(_context)|colorize }}
{% endif %}
