{% trans_default_domain textDomain %}

{{ 'section.default.subtitle'|trans(_context)|colorize }}

{{ 'section.default.say'|trans(_context)|colorize }}

{% if reBuy %}
    {{ 'section.default.rebuy.yes'|trans(_context)|colorize }}
{% else %}
    {{ 'section.default.rebuy.no'|trans(_context)|colorize }}
{% endif %}

{% if pointsAvailable < cost %}
    {{ 'section.default.buy.no'|trans(_context)|colorize }}
{% else %}
    {{ 'section.default.buy.yes'|trans(_context)|colorize }}

    {{ 'section.default.form.current'|trans(_context) }}: {{ regName }},
    {{ 'section.default.form.look'|trans(_context)|colorize }}

    <form action="{{ 'runmodule.php?module=namecolor&op=namepreview'|lotgd_url }}" method="post" class="ui form">
        <div class="field">
            <label for="newname">{{ 'section.default.form.label'|trans|colorize }}</label>
            <input type="text" name="newname" id="newname" value="{{ regName }}">
        </div>
        <div class="ui message">
            {{ 'section.default.form.note'|trans(_context)|colorize }}
        </div>
        <div class="field"><button type="submit" class="ui button">{{ 'section.default.form.submit'|trans|colorize }}</button></div>
    </form>
{% endif %}
