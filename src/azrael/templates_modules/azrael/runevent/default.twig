{% trans_default_domain textDomain %}

{{ 'section.default.paragraph'|trans(_context)|colorize }}

{% if ghosttown %}
    {{ 'section.default.ghost.town'|trans(_context)|colorize }}
{% endif %}

{{ 'section.default.sinister'|trans(_context)|colorize }}

{% if canPay %}
    {{ 'section.default.can.pay.yes'|trans(_context)|colorize }}
{% else %}
    {{ 'section.default.can.pay.no'|trans(_context)|colorize }}
{% endif %}

{{ 'section.default.do'|trans(_context)|colorize }}
