{% trans_default_domain textDomain %}

{{ 'section.trick.paragraph'|t(_context)|colorize }}

{% if bad == 1 %}
    {{ 'section.trick.bad1.paragraph'|t(_context)|colorize }}
    {% if lostCharm %}
        {{ 'section.trick.bad1.charm'|t(_context)|colorize }}
    {% endif %}
{% elseif bad == 2 %}
    {{ 'section.trick.bad2.paragraph'|t(_context)|colorize }}

    {% if not takeGold and not takeGems %}
        {{ 'section.trick.bad2.no.money'|t(_context)|colorize }}
    {% else %}
        {% if takeGold %}
            {{ 'section.trick.bad2.gold'|t(_context)|colorize }}
        {% endif %}

        {% if takeGems %}
            {{ 'section.trick.bad2.gems'|t(_context)|colorize }}
        {% endif %}
    {% endif %}

    {{ 'section.trick.bad2.end'|t(_context)|colorize }}
{% else %}
    {{ 'section.trick.bad.paragraph'|t(_context)|colorize }}
{% endif %}


