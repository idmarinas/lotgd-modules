{% trans_default_domain textDomain %}

{{ 'section.trick.paragraph'|trans(_context)|colorize }}

{% if bad == 1 %}
    {{ 'section.trick.bad1.paragraph'|trans(_context)|colorize }}
    {% if lostCharm %}
        {{ 'section.trick.bad1.charm'|trans(_context)|colorize }}
    {% endif %}
{% elseif bad == 2 %}
    {{ 'section.trick.bad2.paragraph'|trans(_context)|colorize }}

    {% if not takeGold and not takeGems %}
        {{ 'section.trick.bad2.no.money'|trans(_context)|colorize }}
    {% else %}
        {% if takeGold %}
            {{ 'section.trick.bad2.gold'|trans(_context)|colorize }}
        {% endif %}

        {% if takeGems %}
            {{ 'section.trick.bad2.gems'|trans(_context)|colorize }}
        {% endif %}
    {% endif %}

    {{ 'section.trick.bad2.end'|trans(_context)|colorize }}
{% else %}
    {{ 'section.trick.bad'|trans(_context)|colorize }}
{% endif %}


