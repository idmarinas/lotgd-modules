{% trans_default_domain textDomain %}

{{ 'section.buyslot.paragraph'|trans(_context)|colorize }}

{% set fullText = 'chronofiddling.sphere.full'|trans(_context)|uncolorize %}

{% for i in 1..days %}
    <img src="images/daysphere-full.png" alt="{{ fullText }}" title="{{ fullText }}">
{% endfor %}

{% if empty %}
    {% set emptyText = 'chronofiddling.sphere.empty'|trans(_context)|uncolorize %}

    {% for i in 1..empty %}
    <img src="images/daysphere-empty.png" alt="{{ emptyText }}" title="{{ emptyText }}">
    {% endfor %}
{% endif %}

{% if canFillSpheres %}
    {{ 'section.buyslot.fill'|trans(_context)|colorize}}
{% endif %}
