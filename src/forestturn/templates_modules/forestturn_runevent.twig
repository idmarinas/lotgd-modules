{% trans_default_domain textDomain %}

{% if win %}
    {{ 'win.yes'|trans(_context)|colorize }}
{% else %}
    {{ 'win.no'|trans(_context)|colorize }}

    {{ 'lose'|trans(_context)|colorize }}
{% endif %}
