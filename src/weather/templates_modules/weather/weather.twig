{% trans_default_domain textDomain %}

{{ ('weather.' ~ type|default('normal') ~ '.report')|trans(_context|merge({ 'weather': ('weather.' ~ type|default('normal') ~ '.0' ~ weather)|trans }))|colorize }}
