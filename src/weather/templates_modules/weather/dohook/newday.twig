{% trans_default_domain textDomain %}

{{ 'section.newday.weather'|trans(_context|merge({ 'weather': ('weather.normal.0' ~ weatherNormal)|trans}))|colorize }}

{% if gotFight %}
    {{ 'section.newday.got.fight'|trans(_context)|colorize }}
{% endif %}
