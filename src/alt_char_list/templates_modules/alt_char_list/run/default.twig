{% trans_default_domain textDomain %}

<table class="ui very compact striped table">
    <caption>{{ 'title'|trans|colorize }}</caption>
    <thead>
        <tr>
            <th scope="col">{{ 'section.default.table.thead.name'|trans|uncolorize }}</th>
            <th scope="col">{{ 'section.default.table.thead.ip'|trans|uncolorize }}</th>
            <th scope="col">{{ 'section.default.table.thead.id'|trans|uncolorize }}</th>
            <th scope="col">{{ 'section.default.table.thead.email'|trans|uncolorize }}</th>
        </tr>
    </thead>
    <tbody>
        {% for acct in accounts %}
            <tr>
                <td>{{ acct.acct.name|colorize }} ({{ acct.acct.login }})</td>
                <td>
                    {% for rowIp in acct.ip %}
                        {{ rowIp.name|colorize }} ({{ rowIp.login }})<br>
                    {% else %}
                        {{ 'section.default.table.tbody.none'|trans|colorize }}
                    {% endfor %}
                </td>
                <td>
                    {% for rowId in acct.id %}
                        <strong>{{ rowId.name|colorize }}</strong> ({{ rowId.login }})<br>
                    {% else %}
                        {{ 'section.default.table.tbody.none'|trans|colorize }}
                    {% endfor %}
                </td>
                <td>
                    {% for rowEmail in acct.email %}
                        <strong>{{ rowEmail.name|colorize }}</strong> ({{ rowEmail.login }})<br>
                    {% else %}
                        {{ 'section.default.table.tbody.none'|trans|colorize }}
                    {% endfor %}
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ navigation_pagination(paginator) }}
