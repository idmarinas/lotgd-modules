{% trans_default_domain textDomain %}

{% if not seenLover %}
    {% if married %}
        {% if flirtSuccess %}
            {{ 'section.run.married.bard.success'|trans(_context)|colorize }}
        {% else %}
            {{ 'section.run.married.bard.fail.paragraph'|trans(_context|merge({ 'msg': ('section.run.married.bard.fail.msg.0' ~ random(1, 4))|trans(_context) }))|colorize }}
        {% endif %}
    {% else %}
        {% if flirt %}
            {% if flirt == 7 %}
                {{ 'section.run.flirt.bard.case.07.paragraph'|trans(_context)|colorize }}
            {% endif %}

            {{ ('section.run.flirt.bard.case.' ~ flirt ~ '.' ~ flirtCase)|trans(_context)|colorize }}

            {% if charmGain %}
                {{ 'section.run.flirt.bard.charm.gain'|trans(_context)|colorize }}
            {% endif %}

            {% if charmLost %}
                {{ 'section.run.flirt.bard.charm.lost'|trans(_context)|colorize }}
            {% endif %}
        {% endif %}
    {% endif %}
{% else %}
    {{ 'section.run.flirt.bard.seen'|trans(_context)|colorize }}
{% endif %}
