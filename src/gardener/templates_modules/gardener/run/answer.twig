{% trans_default_domain textDomain %}

{% if correct %}
    {{ 'section.answer.correct'|trans|colorize }}

    {% if reward %}
        {{ 'section.answer.reward.gold'|trans(_context)|colorize }}
    {% else %}
        {{ 'section.answer.reward.gem'|trans(_context)|colorize }}
    {% endif %}
{% else %}
    {{ 'section.answer.uncorrect'|trans|colorize }}
{% endif %}
