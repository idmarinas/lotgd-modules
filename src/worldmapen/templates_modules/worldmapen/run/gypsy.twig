{% trans_default_domain textDomain %}

{% if buyed is same as(true) %}
    {{ 'section.gypsy.buy.yes'|trans(_context)|colorize }}
{% elseif buyed is same as(false) %}
    {{ 'section.gypsy.buy.no'|trans(_context)|colorize }}
{% else %}
    {{ 'section.gypsy.enter'|trans(_context)|colorize }}
{% endif %}
