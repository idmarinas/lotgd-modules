{% trans_default_domain textDomain %}

{% if not battle %}
    {{ 'section.fight.paragraph'|t(_context)|colorize }}

    {% if showSmallMap %}
        {{ module_worldmapen_show_mini_map(mapLinks) }}
    {% endif %}

    {{ commentary_block({
    'section': 'mapchat-' ~ mapLoc
    }, textDomain) }}

    {{ module_worldmapen_show_legend(true) }}
{% endif %}
